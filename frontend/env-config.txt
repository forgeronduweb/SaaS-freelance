# Configuration d'environnement pour AfriLance Backend
# Copiez ce contenu dans un fichier .env.local

# Base de données MongoDB
DATABASE_URL="mongodb://localhost:27017/afrilance"
# Ou pour MongoDB Atlas : 
# DATABASE_URL="mongodb+srv://username:password@cluster.mongodb.net/afrilance"

# JWT Secret (générez une clé secrète forte)
JWT_SECRET="votre-cle-secrete-jwt-tres-longue-et-complexe"

# NextAuth Secret (pour l'authentification)
NEXTAUTH_SECRET="votre-cle-nextauth-secrete"
NEXTAUTH_URL="http://localhost:3000"

# Variables optionnelles pour les paiements
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Configuration email (pour reset password)
EMAIL_FROM="noreply@afrilance.com"
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASSWORD="your-app-password"
